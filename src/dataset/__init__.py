"""
Dataset construction, tracking issue detection, label processing,
and DINOv3 embedding extraction from tracked objects.

This package re-exports all public names from its submodules so that
``from src.dataset import ...`` continues to work unchanged.
"""
