# GUI module

As of 2026-04-14, the GUI module (`src/gui/`) is under active development on the `gui` branch. PySide6-based; has its own pixi env (`gui`).

Do not modify GUI-related code on `main` -- changes should target the `gui` branch.
